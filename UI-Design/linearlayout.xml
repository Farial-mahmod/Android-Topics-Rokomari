/*
a LinearLayout is a viewgroup that sets its children views in one single direction - either vertically or horizontally .
android:orientation to specify direction.
android:layout_weight gives importance to a view in terms of space.
Equal Distribution: android:layout_height="0dp" (vertical) or android:layout_width="0dp" (horizontal) .
*/

<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:orientation="vertical" >
  
  
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/name" />
  
    <EditText
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="top"
        android:hint="@string/message" />
  
    <Button
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/send" />
  
</LinearLayout>
